[build-system]
requires = ["setuptools", "wheel", "setuptools_scm"]
build-backend = "setuptools.build_meta"


[tool.setuptools_scm]
write_to = "src/napari_crop_and_mask/_version.py"

[tool.black]
line-length = 120

[tool.isort]
profile = "black"
line_length = 120
